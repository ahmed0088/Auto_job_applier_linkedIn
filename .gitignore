__pycache__
.pytest_cache
.vscode
.idea

all excels
all resumes
logs

old

secrets.py
config/personals.py
.venv
apikeys.py

# Local control-panel runtime files — contain your credentials/answers, never commit
user_config.json
.bot_run.log
.bot_run.pid
.DS_Store

# Per-profile settings/resume/history (see config/_overrides.py) — contain
# credentials and personal data for every profile, never commit
profiles
.active_profile
